# Lab 2 AI Use and Reflection

I used ZCODE Harness + VSCODE. Model: [Deepseek V4 Flash 0731]. Thinking level: [Max].

Selected Key Prompts:

| Prompt Name | Actual Prompt Text | My Reflection |
|---|---|---|
| Plan Lab 2 Database Schema | Read the enclosed Lab 2 specification and the existing Lab 1 Prisma schema. List every new model, enum, relationship, and seed record needed. Propose the migration order and any idempotency strategy. Do not write code yet. | I provided the Lab 2 specification and the existing Lab 1 schema to establish the data model boundaries. This was important because the new models (Requester, Ticket, Attachment, RelatedSystem) depend on each other and on the existing Category table. |
| Design Issue 2 Schema | Create the Prisma schema for Issue 2: Requester, Ticket with Status/Priority enums, Attachment with soft-removal, RelatedSystem, and a nullable Ticket.systemId FK to RelatedSystem (One-to-Many). Include all indexes from the specification. Reuse Category unchanged. | The AI drafted the full schema. I corrected the ticketNumber column to VARCHAR(20) to match the TTK-YYYY-000000 format, ensured all foreign keys use Restrict for Ticket.requesterId/categoryId, and verified Cascade on Attachment deletes. |
| Implement Idempotent Seed | Write an idempotent Prisma seed script for Issue 2. Seed: 4 categories (Lab 1), 4 active Development Requesters (Alpha-Delta), 1 inactive Requester (Epsilon), and 7 Related Systems. Use upsert keyed on unique fields. | I specified the exact seed data from the labsheet and enforced idempotency via upsert. The AI correctly used email for requesters and name for categories/systems. I verified Epsilon is seeded with isActive: false for the 403 test path. |
| Write Migration SQL | Write the initial migration SQL for Issue 2 that creates all new tables, enums, indexes, and seed inserts. Ensure it is safe to run on a clean PostgreSQL database and preserves the existing Category table. | The AI generated the raw SQL migration. I reviewed it for correct enum creation, foreign key constraints, the Ticket.systemId FK (which requires RelatedSystem to be created before Ticket), and the ticket_number_seq. I added ON CONFLICT clauses to the seed inserts so the migration is idempotent. |
| Implement API for Development Requester | Implement GET /api/requesters for Issue 3 per api-spec 2.3: return only active Requesters ordered by id, excluding the inactive Epsilon seed. Add the API-21 test and keep the safe 500 error envelope consistent with the existing /api/categories route. | I gave the agent the exact api-spec shape and BR-15 semantics so the endpoint and its Supertest test matched the contract. I verified the endpoint returned exactly the 4 active seeded requesters over HTTP and that the inactive Epsilon requester was excluded. |
| Build Dev Requester Selector and Context | Build the Issue 3 client slice: a Development Requester selector in the app-shell navbar with the required "Testing only — not real authentication" caption, a context that loads requesters, defaults to the first active one, and persists the selection to localStorage (FR-13, BR-03, BR-05). Cover loading, rendering, switching persistence, restore, and error+retry with UI-07 tests. | The agent implemented the provider/context split (DevRequesterContext + DevRequesterProvider) to keep Fast Refresh clean, added Zen Green shell tokens per ui-spec 1, and wrote the UI-07 tests. I reviewed the split and the localStorage key, and verified the whole client suite, lint, and build stayed green. |
| Execute Issue #13 — Ticket Creation API + GAP fixes | Global Engineering Protocol active. On branch feat/lab2-create-ticket-api: 1) GAP-A make relatedSystemId required (new Prisma migration + schema + GET /api/related-systems + docs), GAP-B/C audit values to 5/4/4+ and confirm BR-17/18; 2) write failing tests first at the mandated paths (server/tests/lab-02/create-ticket.api.test.ts API-01..06, unit/ticketNumber.test.ts UT-01); 3) implement header middleware, POST /api/tickets with sequence-based ticketNumber, 400/401/403/500 envelope; 4) verify, update tests.md statuses, move Kanban #13 to Started, then STOP and ask before opening/merging the PR. | The agent applied GAP-A required-related-system as a new Prisma migration but kept the existing POST /api/tickets implementation and endpoint/validation behavior that already matched STEP 3 (including the header 400/401/403 paths and the ticket_number_seq). It wrote the API tests at the mandated create-ticket.api.test.ts path and a new unit test for the extracted buildTicketNumber formatter. The tests passed immediately because the endpoint and GAP changes were already committed on this branch, so the 'fail first' step was a documentation-true check rather than a red-green cycle; I flagged that deviation honestly. I also caught the agent labelling API-22 as Pass without a test and had it write the real related-systems test. |

---

## My Reflection

The AI coding agent significantly accelerated the implementation of Issue 2's database schema. What would have been tedious manual schema design was condensed into a single prompt-response cycle where I provided the Lab 2 specification and the AI generated the complete Prisma schema and migration SQL. The AI was particularly effective at generating the raw SQL migration with correct enum types, foreign key constraints, and indexes.

However, I retained full responsibility for the data model. I corrected the ticketNumber column type to VARCHAR(20) to match the TTK-YYYY-000000 format, verified that Cascade deletes were only applied where appropriate (Attachment), and ensured Restrict was used for requesterId, categoryId, and systemId to prevent accidental data loss. I also added the ON CONFLICT clauses to the seed inserts to make the migration idempotent.

In conclusion, the AI served as an efficient schema generator, but the final schema reflects my judgment about referential integrity, naming conventions, and seed idempotency. Every model, enum, and index was reviewed against the Lab 2 specification to ensure correctness.

For Issue 3 (Development Requester context and selector), the AI accelerated turning the API and UI specs into working code and tests. I kept responsibility for the data-access contract (only active requesters, BR-15), the required selector caption (BR-03), and confirming that the full test suite, lint, and build stayed green after the refactor. The AI was especially useful for splitting the context into files to respect React Fast Refresh rules and for adapting the Lab 1 test stubs to URL-routed mocks once the app began loading requesters on mount.

Issue 14 (Create Ticket UI) prompt: "GLOBAL PROTOCOL ACTIVE. Execute Issue #14 — Create Ticket UI. Branch: feat/lab2-create-ticket-ui. Move #14 to Started. STEP 1 — failing tests first at client/tests/lab-02/CreateTicket.test.tsx (UI-01..03 + AC-20). STEP 2 — implement per ui-spec: app shell nav, form with read-only Requester/Ticket fields, Category/Related System/Priority selects, inline validation identical to server rules, attachment picker with 4-MIME allowlist / 5 MB / 5-cap pending state, busy submit with double-click guard and navigate-to-detail on success. STEP 3 — verify client tests, update tests.md, STOP before PR." Also: record the merged PR #25 review conversation into reviewer.md.

Reflection: the AI wrote the 9 failing tests first (all red for the right reason — no New Ticket route existed), then implemented the shell nav with hash routing, CreateTicketPage, AttachmentPicker (pending files only — upload wiring deferred to Issue #17 as instructed), and ticketValidation mirroring BR-11. During the red→green cycle three issues surfaced: React needs `input` events (not `change`) for programmatically set values, userEvent.upload silently enforces the accept attribute so rejection paths must be driven with fireEvent.change like drag-and-drop would, and a real bug in my picker where error state was computed inside a functional setState updater. The AI also had to preserve the exact Lab 1 offline wording ("System Status: Offline" / "Unable to connect to TokTickIT API") when refactoring App.tsx into the shell — a good reminder that docs and existing tests are the contract. Final state: 16/16 client tests green, lint clean, build clean, server suite untouched at 26/26.
Pixel-replication prompt (Issue #14 refactor): "GLOBAL PROTOCOL ACTIVE. Execute Issue #14 — Create Ticket UI, pixel-replication pass. The approved visual target is docs/mockups/create-ticket.html (mockup wins for visuals; api-spec/specification win for behavior). Lift the mockup style block unchanged into client/src/styles/zen-green.css (--tok-* tokens, .tok-* classes). Port the app header (tok-navbar/brand/nav/requester), the Create screen DOM in exact order (breadcrumb, H1, card with Requester context/Classification/Details/Attachments sections, no ticket-number/date fields), measurements (radius, dividers, grid, controls, chips, dropzone, buttons), behavior (client validation identical to server, picker allowlist/5MB/5-cap with mock wording, busy submit, success alert + navigate to detail, Cancel reset), screenshot evidence at 1280/800/375 + six states into artifacts, extend CreateTicket tests, update tests.md/ai-use.md, then STOP and ask about the PR."

Reflection: the mockup file lived at docs/mockups/create-ticket.html (not docs/lab-02/mockups/) and is to be referenced only, never committed. I lifted the mockup's <style> verbatim (minus the trailing </style> token my first extraction leaked, which broke the Vite CSS build and was caught by the build gate). The React app now uses the exact tok-* classes and DOM order, with the TTK number appearing only after a successful POST (success alert + navigate to /tickets/<number>). Accessibility: replaced aria-labelledby with a direct aria-label on the requester select so jsdom queries by name work, while preserving the mock's two-line visual label. Tests stayed green after switching queries to roles/regex (brand, textbox, detail route). Failure state verified: error banner + Try again + inputs preserved. Evidence captures: responsive trio (1280/800/375), state-initial/validation/api-failure, mockup reference — all saved under artifacts/lab-02/screenshots/create-ticket/. The busy-submit and invalid-attachment chips are covered by the automated tests (IAB cannot drive file uploads, and the live-API busy window is too fast to screenshot deterministically; noted honestly rather than faking evidence).

Issue #15 (My Tickets API) prompt: "GLOBAL PROTOCOL ACTIVE. TASK: Issue #15 — My Tickets API. Branch feat/lab2-my-tickets-api from lab2-staging; move #15 to Started. STEP 1 — FAILING TESTS FIRST (exact files): server/tests/lab-02/my-tickets.api.test.ts implementing API-07 (two requesters with data; list returns only active requester's tickets; totalItems counts only theirs), API-08 (case-insensitive search over title OR description; empty search returns all; page resets to 1), API-09 (categoryId + priority combine with AND), API-10 (title asc ordering; priority desc ordering by rank Critical>High>Medium>Low; invalid sortBy/sortDir -> 400), API-11 (25 tickets, page 2 default size slice + correct meta; page=0 -> 400; pageSize=51 -> 400; nonexistent categoryId -> 400), API-20 (simulated DB failure -> 500 generic envelope, no stack trace); server/tests/lab-02/unit/priorityRank.test.ts (UT-02) and server/tests/lab-02/unit/search.test.ts (UT-03). Run; confirm red for the right reasons. STEP 2 — IMPLEMENT GET /api/tickets per api-spec §3.2 (base WHERE requesterId, page/pageSize/search/categoryId/priority/sortBy/sortDir with defaults and validation, { data, meta } envelope, composite index use, do not touch UI). STEP 3 — green server tests; tests.md statuses + §6; STOP and ask about the PR (Closes #15)."

Reflection: I mandated the exact test files, the reserved TTK-9xxxxx fixture band to avoid sequence collisions, and the red-first commit so the 404-based failures were provable. During the cycle the AI hit three real issues I had to adjudicate: Prisma's bare-NULL-parameter trick failing PostgreSQL's 42P18 typed-parameter check (fixed by composing typed SQL fragments), Prisma enum ordering being alphabetical rather than by rank (fixed with a CASE rank expression plus a Prisma-side hydration pass), and a ~1-in-10 flaky full-suite failure that we chased to parallel Vitest workers interleaving writes across files on the shared database — resolved with fileParallelism: false, the serial execution tests.md §5 anticipated, and verified over 16 consecutive runs. The API-20 stub point moved from $queryRaw to $transaction deliberately so the documented simulation hook keeps working. UI untouched per scope.

Issue #15 review follow-up (PR #27) prompt: "ดู PR comment และเช็คให้หน่อยว่าจริงไหม ถ้าจริงก็แก้ ถ้าไม่จริงก็ไม่ต้องแก้ และบอกผมว่าควรตอบกลับยังไง" (reviewer Alongkron1234 asked whether %, _, and backslash are escaped before ILIKE in ticket.service.ts, saying they could not find it).

Reflection: The reviewer's instinct was right even though half their premise was not — the page query DID escape (ticket.service.ts buildIlikePattern), but chasing the claim surfaced a real bug they had not named: the COUNT query used Prisma contains without escaping, so search terms containing % or _ produced data rows and totalItems that disagreed. Proved with a live probe (search=% → 1 row, totalItems=3), wrote a failing wildcard-consistency test (expected 3 to be 1), fixed by sharing one escaped Prisma.Sql fragment across both queries in the transaction, suite now 60/60 over multiple runs. Lesson recorded: when a reviewer says "I could not find X", verify X but also audit its sibling code paths for the same concern.

Issue #16 (My Tickets UI) prompt: "GLOBAL PROTOCOL ACTIVE. TASK: Issue #16 — My Tickets UI. Branch feat/lab2-my-tickets-ui. STEP 1 — docs patches first (ui-spec §10, tests.md §2.3 test paths). STEP 2 — FAILING TESTS FIRST at client/tests/lab-02/MyTickets.test.tsx covering UI-04 (skeleton while loading then rows; error banner + Try again), UI-05 ("No tickets yet" vs "No results match your filters"), UI-06 (debounced search 300ms; category/priority/sort issue correct API params incl. all six sort labels; Clear filters appears only when non-default and resets), UI-10 (failure preserves filter state; retry re-fetches same params), UI-11 (accessibility: column headers, text-label badges for all four priorities + status New with dot glyph, monospace ticket-number links with accessible names, aria-live busy region, aria-current pagination). Implement per labsheet §8.4 + ui-spec §10-11: toolbar (search with magnifier + clear ×), 6-column table (Category hidden ≤991px as inline chip in title cell), mobile card links <768px, pagination "Showing X–Y of Z" with numbered window + ellipsis and Prev/Next bounds, priority/status pill badges with exact colors from docs/mockups/MyTicket-NoHeader.html (the visual contract; header unchanged). STEP 3 — green tests, full suite, build gate."

Reflection: the AI ran a true red→green cycle: all 11 initial tests failed for the right reason (the /tickets route still rendered the placeholder), then went green after implementing MyTicketsPage.tsx + my-tickets.css mirroring the mockup, finishing at 13/13 in the file and 29/29 across the client suite. Two QA-review findings were real and fixed: BR-05 requester switching refetched with the previous requester's filters/page (a stale page>1 against a smaller dataset rendered a blank region) — resolved by keying MyTicketsPage by activeRequester.id so a switch remounts clean; and the pagination window emitted six buttons when totalPages was exactly 6 — capped at five numbered buttons whenever totalPages > 5. Docs-first patches landed before code: ui-spec §10 corrected from 7 to 6 columns (Related System filter/column removed — matches Issue #16 scope, mockup, and the shipped API) plus §7 wording, and the tests.md Automated Test File column repointed to MyTickets.test.tsx. Evidence screenshots were generated with Playwright against the live stack (postgres + server + client on 5173) via client/scripts/my-tickets-shots.mjs into artifacts/lab-02/screenshots/my-tickets/ (desktop/tablet/mobile + initial/api-failure/no-results/empty states); the ZCode in-app browser capture was unusable at small viewports, so the repo's own Playwright was used instead.

Issue #30 (My Tickets v2 UI + additive API extensions) prompt: "GLOBAL PROTOCOL ACTIVE. TASK: Issue #30 — My Tickets v2 UI + additive API extensions. 1) Create the GitHub Issue, add to project (Backlog→Specified→Started), branch feat/lab2-my-tickets-v2 from lab2-staging (renamed to feat/create-ui-api-fix on request). 2) Docs-first patches (specification.md FR-18/BR-20..22/AC-23/D-16..17, api-spec §3.2, ui-spec §10 v2, tests.md rows). 3) STEP 1 backend additive: Prisma migration extending Status enum (keep NEW) + nullable itPriority/ownerName; idempotent seed (Alpha=42 at pageSize 8 → 6 pages, Beta/Gamma/Delta=10, statuses varied, itPriority sometimes equal/different/null, ownerName from fixed pool with some null, ticketNumber via backend generator); GET /api/tickets extensions (itPriority/status filters invalid→400, search matches ticketNumber OR title OR description case-insensitive, sortBy accepts ticketNumber) with api-spec §3.2 updated. 4) Server tests API-22..24 in my-tickets.api.test.ts (count-sensitive assertions use test-created requesters). 5) STEP 2 frontend: faithful port of docs/mockups/my-tickets-v032.html — page head (subtitle + Clear Filters secondary refresh icon + Create Ticket primary plus icon), filter card (search placeholder 'Search by ticket number or summary.' debounced 300ms with clear ×, labeled Category / Requested Priority / IT Priority / Current Status selects), fluid nine-column table (Ticket No./Created Date/Summary/Category/Requested Priority/IT Priority/Current Status/Ticket Owner/Last Updated; sortable headers with stacked carets + aria-sort cycling; switching column resets to natural default — ticketNumber asc, dates desc), badges identical to the reference (pri-low/medium/high/critical incl '! ' Critical; st-open/pending/inprogress/resolved; null itPriority → 'Unset', null ownerName → muted 'Unassigned'), pagination footer 'Showing X to Y of Z tickets' aria-live + window 1..5+ellipsis+last / ‹Previous/Next› with active solid green + aria-current, page change scrolls to top; live states (3 shimmer skeleton rows; 'No tickets yet' + CTA; 'No results match your filters' + Clear filters; failure banner + tertiary Try again preserving filters); <768px stacked m-cards (row1 ticket link + created date, summary, 3-badge row, meta 'Category · Owner' + 'Updated <date>', ≥44px targets, single-column filters, no horizontal scroll); 768–991px keep ALL nine columns with reduced type. Rewrite client/tests/lab-02/MyTickets.test.tsx covering UI-04..06 plus UI-12 (new filters issue correct params + page reset), UI-13 (sortable headers cycle + aria-sort), UI-14 (pagination window + showing text), UI-15, keeping UI-11 a11y. 6) E2E: extend e2e with a My Tickets v2 block (Category + IT Priority + Status AND filter, search by ticket number, sort Ticket No. asc, paginate to page 2 asserting seeded rows + showing text, at 375px cards render with scrollWidth ≤ innerWidth). 7) Refresh screenshots into artifacts/lab-02/screenshots/my-tickets/ (desktop-1280/tablet-800/mobile-375). 8) All gates: server npm test; client vitest; client npm run test:e2e; server + client build; client lint. 9) Update tests.md Final Status rows + §6 and append this prompt to ai-use.md. 10) STOP and ask before opening the PR."

Reflection: the branch name was renamed to feat/create-ui-api-fix on request after creation. Two test-ID collisions surfaced and were resolved docs-first: API-22..24 and UI-12 were already allocated to Related System rows, so the new rows use API-26..28 / UI-13..15 (recorded in tests.md and api-spec). Real bug found while refreshing evidence: the Docker containers (fulllab-server/client) were still built from the pre-v2 image, so GET /api/tickets returned 500 with "Value 'OPEN' not found in enum 'Status'" — the container's baked Prisma client predated the migration; fixed by `docker compose up -d --build server client`. A second real bug surfaced in the v2 frontend: sorting/filtering set status=loading on every refetch, which unmounted the table and detached the just-clicked header (stale aria-sort in the jsdom tests was the symptom) — fixed by keeping status='ready' during background refreshes so the skeleton only shows on initial load/error retry. E2E fixtures were routed to Dev User Delta so Alpha=42/Beta=10 stay deterministic for the responsive/ownership assertions; the v1 shot script's 'state-empty' requester assumption no longer holds (every seeded requester now has demo tickets), so the script inserts a throwaway 'Shot Empty User' (id 9001) for that screenshot. E2E also had to drop a call to the not-yet-existing ticket-detail endpoint (Issue #17) and assert list-level isolation instead. Screenshots were regenerated against the rebuilt containers.
