{
  "name": "toktickit",
  "version": "0.0.0",
  "private": true,
  "description": "TokTickIT monorepo root helper scripts (CPE334 Lab 1)",
  "scripts": {
    "db:migrate": "npm --prefix server run prisma:migrate",
    "db:seed": "npm --prefix server run prisma:seed"
  }
}